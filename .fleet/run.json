{
	"configurations": [
		{
			"name": "Build",
			"type": "command",
			"workingDir": "$WORKSPACE_DIR$/build/",
			"program": "/usr/bin/cmake",
			"args": [".."]
		},
		{
			"name": "Run",
			"type": "command",
			"dependsOn": ["Build"],
			"workingDir": "$WORKSPACE_DIR$/build/",
			"program": "./better-than-nothing"
		}
	]
}
